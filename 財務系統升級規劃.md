# 訂單異動紀錄與財務記帳系統規劃

這是一個非常關鍵且專業的營運需求！當營區規模變大、經手的人變多，或是導入多種金流（現金、匯款、綠界信用卡）時，原本「單純覆寫數字」的做法絕對不夠用，必須導入**「歷史軌跡（Audit Log）」**與**「收款分類帳（Payment Ledger）」**的概念。

本計畫**目前處於存檔備用階段**，未來當您決定要啟動時，直接將此計畫貼給我，即可立刻動工！

---

## 1. 資料庫結構升級規劃 (Database Schema)

我們將新增兩張專屬的資料表，讓訂單的每一個動作與每一筆錢都清清楚楚。

### A. 新增「訂單異動紀錄表」 (`nf_order_logs`)
**用途：** 記錄「誰、在什麼時候、改了什麼資料」。
* **`id`**: 紀錄編號
* **`order_id`**: 關聯的訂單
* **`operator_name`**: 操作者姓名 (直接抓取當前登入後台的管理員帳號名稱，例如：`營主A`、或 `綠界系統自動`)
* **`action_type`**: 動作類型 (例如：`修改總金額`、`修改定金`、`更改狀態`、`更新備註`)
* **`details`**: 詳細異動內容 (JSON格式，例如：`{ "舊金額": 5000, "新金額": 4500 }`)
* **`created_at`**: 異動時間

### B. 新增「收款明細表」 (`nf_order_payments`)
**用途：** 取代原本單一的 `deposit_amount` 欄位。一筆訂單可以有多筆收款紀錄。
* **`id`**: 收款編號
* **`order_id`**: 關聯的訂單
* **`amount`**: 收款金額
* **`payment_stage`**: 收款階段 (分為：`定金`、`尾款`、`全額`)
* **`payment_method`**: 收款方式 (預留完整選項：`銀行匯款`、`現場現金`、`綠界_信用卡`、`綠界_ATM` 等)
* **`handled_by`**: 經手人 (抓取登入管理員名稱，或綠界系統)
* **`transaction_info`**: 交易明細 (例如：匯款後五碼、綠界交易序號)
* **`created_at`**: 收款時間

---

## 2. 後台介面 (UI) 升級規劃

### A. 獨立的「財務收款區塊」
* 在訂單明細中，不再只是單純輸入一個定金數字。
* 會有一顆按鈕叫做 **「➕ 新增收款紀錄」**。
* 點擊後，營主可以輸入金額、選擇方式，系統會自動帶入「當前登入的帳號名字」作為經手人。
* 系統會自動加總所有收款紀錄，計算出「已收總額」與「尚欠尾款」。

### B. 訂單專屬的「歷史軌跡 (Timeline)」
* 在訂單的最下方或側邊，增加一個 **「訂單異動軌跡」** 的時間軸區塊。
* 清楚顯示幾月幾號、哪位管理員、新增了什麼收款或是修改了什麼總金額。

### C. 綠界金流 (ECPay) 預留機制
* 雖然目前綠界已申請通過但尚未正式啟用付款，資料庫架構會**提前把綠界所需的欄位 (例如交易序號、刷卡方式) 全部開好**。
* 未來綠界正式上線時，系統會自動在背景將綠界的回傳結果寫入 `nf_order_payments` 表，無縫接軌。

---

## 3. 未來多營地擴展與複製策略 (Multi-Tenant / Copy Strategy)

未來的願景是這套系統可以輕易地「複製」給不同的營地使用。針對這點，最簡單且最乾淨的架構做法如下：

### A. 獨立資料庫 (Database per Campsite)
* **做法：** 每當有一個新營地，我們就在 Supabase (或是任何資料庫服務) 上面**開一個全新的 Project (全新資料庫)**。
* **優點：** 資料絕對不會混在一起，每個營地都有自己獨立的訂單、客人名單、庫存表。這符合最高資安標準，不會因為寫錯程式碼導致 A 營地看到 B 營地的訂單。

### B. 程式碼共用與環境變數切換
* **做法：** 程式碼 (Frontend / Backend) **完全不用改寫**！這套系統的程式碼是通用的。
* 當部署一個新營地的網站時 (例如部署到 Vercel)，我們只需要在後台設定該營地專屬的 **「環境變數 (Environment Variables)」**：
  * `VITE_SUPABASE_URL` = (新營地的資料庫網址)
  * `VITE_SUPABASE_ANON_KEY` = (新營地的資料庫金鑰)
  * `LINE_LIFF_ID` = (新營地專屬的 LINE 登入 ID)
* 系統一啟動，就會自動連線到該營地的專屬資料庫。

### C. 部署流程SOP
未來要幫新營地架設系統的流程非常簡單：
1. **建立新資料庫：** 在 Supabase 點擊建立新專案，並執行我們準備好的 `schema.sql` (裡面包含了建表、訂單、帳務紀錄表的語法)。
2. **複製前端專案：** 在 Vercel 複製現有專案。
3. **填入環境變數：** 把步驟 1 拿到的 URL 和 Key 填進 Vercel。
4. **完成上線：** 一個擁有完整前後台、金流紀錄的全新營地系統就誕生了！
